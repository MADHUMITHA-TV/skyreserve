# Test Database
DATABASE_URL="mysql://root:root@localhost:3306/skyreserve_test"

# JWT
JWT_SECRET=test_jwt_secret
JWT_EXPIRES_IN=1h

JWT_REFRESH_SECRET=test_refresh_secret
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=5001
NODE_ENV=test

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://127.0.0.1:6379

# Mail (Dummy)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
EMAIL_FROM=test@skyreserve.com

# Frontend
CLIENT_URL=http://localhost:3000